---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/Hero.astro";
import About from "../components/About.astro";
import PracticeAreas from "../components/PracticeAreas.astro";
import Differentiators from "../components/Differentiators.astro";
import FAQ from "../components/FAQ.astro";
import Contact from "../components/Contact.astro";
import WhatsAppFloat from "../components/WhatsAppFloat.astro";
import Header from "../components/Header.astro";
import site from "../data/site.json";

const { seo, contact, hero, sobre, areas, diferenciais, faq, contato, nav } = site;
---

<Layout
  title={seo.title}
  description={seo.description}
  area={seo.area}
  city={seo.city}
  lawyerName={contact.lawyerName}
  phone={contact.phone}
  email={contact.email}
  address={contact.address}
  openingHours={contact.openingHours}
  sameAs={contact.sameAs}
>
  <Header links={nav.links} />
  <main id="main-content">
    <Hero
      headline={hero.headline}
      subheadline={hero.subheadline}
      ctaText={hero.ctaText}
      trustBadge={hero.trustBadge}
      whatsappNumber={contact.whatsappNumber}
      whatsappMessage={contact.whatsappMessage}
    />
    <About
      title={sobre.title}
      bio={sobre.bio}
    />
    <PracticeAreas
      title={areas.title}
      whatsappNumber={contact.whatsappNumber}
      whatsappMessage={contact.whatsappMessage}
    />
    <Differentiators title={diferenciais.title} items={diferenciais.items} />
    <FAQ title={faq.title} items={faq.items} />
    <Contact
      title={contato.title}
      subtitle={contato.subtitle}
      whatsappButtonText={contato.whatsappButtonText}
      whatsappNumber={contact.whatsappNumber}
      whatsappMessage={contact.whatsappMessage}
      phone={contact.phone}
      email={contact.email}
      address={contact.address}
    />
  </main>
  <WhatsAppFloat
    whatsappNumber={contact.whatsappNumber}
    whatsappMessage={contact.whatsappMessage}
  />
</Layout>
